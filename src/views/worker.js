export function renderWorker() {
  return `
    <main class="dashboard-worker">
      <h2>Bienvenido trabajador</h2>

      <section id="all-pets-container">
        <!-- Aquí se cargarán todas las mascotas de todos los usuarios -->
      </section>

      <button id="logout">Cerrar sesión</button>
    </main>
  `;
}